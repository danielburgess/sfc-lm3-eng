# cutscene-bytecode-2

# LM3-specific scalars (top-level for extras)
note = "bytecodes with absolute addrs — cannot relocate"

[table]
name = "cutscene-bytecode-2"
type = "pointer"

[encoding]
table_file = "en_data/eng.tbl"
fallback = "jp_data/jap.tbl"
terminator = 0x00

[pointers]
address = 0x50010
count = 120
size = 2

[data]
start = 0x50101
