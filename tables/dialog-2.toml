# dialog-2

# LM3-specific scalars (top-level for extras)
dialog_group = 0x1

[table]
name = "dialog-2"
type = "pointer"

[encoding]
table_file = "en_data/eng.tbl"
fallback = "jp_data/jap.tbl"
terminator = 0x00

[pointers]
offset = 0x1B8000
count = 196
size = 2

[_disabled_section]
